# Verification snapshot

These reports record the successful four-recording conversion on September 14, 2026 Pacific time (September 15 UTC).
Absolute local machine paths have been replaced by placeholders and invocation examples use `python`; measured values, timestamps and hashes are unchanged.
They document the original run, not a GitHub Actions run. Fresh runs write separate evidence into `run-reports/`.
`CHECKS.md` records six deliberately corrupted temporary copies plus one normal-data control and a byte-identical repeat conversion.
